export const state = () => ({
    // Tambahkan state awal jika diperlukan
  });
  
  export const mutations = {
    // Tambahkan mutations jika diperlukan
  };
  
  export const actions = {
    // Tambahkan actions jika diperlukan
  };
  